@model IEnumerable<MyNotesMVC.Models.LinkedArticle>

@{ 
    string astring = ViewData["articleClass"].ToString();
    string classString = "list-group-item list-group-item-info " + astring;
}
    @foreach (var linkedArticle in Model)
    {
        
            
     
    }
